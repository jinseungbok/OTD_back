<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.otd.onetoday_back.diary.DiaryMapper">

    <resultMap id="DiaryResultMap" type="com.otd.onetoday_back.diary.model.DiaryGetRes">
        <id property="diaryId" column="diary_id"/>
        <result property="diaryName" column="diary_name"/>
        <result property="diaryContent" column="diary_content"/>
        <result property="diaryImage" column="diary_image"/>
        <result property="createdAt" column="created_at"/>
        <result property="mood" column="mood"/>
    </resultMap>

    <select id="findAll"
            parameterType="com.otd.onetoday_back.diary.model.DiaryGetReq"
            resultMap="DiaryResultMap">
        SELECT *
        FROM diary
        WHERE member_no_login = #{memberNoLogin}
        ORDER BY created_at DESC
            LIMIT #{offset}, #{pageSize}
    </select>

    <select id="getTotalCount"
            parameterType="com.otd.onetoday_back.diary.model.DiaryGetReq"
            resultType="int">
        SELECT COUNT(*)
        FROM diary
        WHERE member_no_login = #{memberNoLogin}
    </select>

    <select id="findById"
            parameterType="map"
            resultMap="DiaryResultMap">
        SELECT *
        FROM diary
        WHERE diary_id = #{diaryId}
          AND member_no_login = #{memberNoLogin}
    </select>

    <insert id="save"
            parameterType="com.otd.onetoday_back.diary.model.DiaryPostReq"
            useGeneratedKeys="true"
            keyProperty="diaryId">
        INSERT INTO diary (
            member_no_login,
            diary_name,
            diary_content,
            diary_image,
            mood,
            created_at
        ) VALUES (
                     #{memberNoLogin},
                     #{diaryName},
                     #{diaryContent},
                     #{diaryImage},
                     #{mood},
                     NOW()
                 )
    </insert>

    <update id="update"
            parameterType="com.otd.onetoday_back.diary.model.DiaryPutReq">
        UPDATE diary
        SET diary_name = #{diaryName},
            diary_content = #{diaryContent},
            diary_image = #{diaryImage},
            mood = #{mood}
        WHERE diary_id = #{diaryId}
          AND member_no_login = #{memberNoLogin}
    </update>

    <delete id="delete"
            parameterType="map">
        DELETE FROM diary
        WHERE diary_id = #{diaryId}
          AND member_no_login = #{memberNoLogin}
    </delete>

</mapper>